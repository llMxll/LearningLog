def factorial(value):
  if value <= 1:
    return 1
  else:  
    return(value*factorial(value - 1))
    

print("""Factorial Finder

Input a numebr""")
value = int(input("> "))

print(f"\nThe factorial of {value} is {factorial(value)}")
